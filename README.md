директория в этой ветке - то что скачается после запуска скрипта. В ней видимо и будем работать.

эту ветку предлагаю использовать основной, от нее ветвить.

# init guide:

клон репозитория
копирование скрипта из мейна (взято с сайта с датасетом)
переключение на ветку installed
скрипт запустить
._* файлы можно удалить

после этого всего поидее состояние репозитория у нас будет одинаковое и можно спокойно пулить/пушить как обычно,
потому что датасет уже скачан а кроме него в гитигноре ничего нет


далее наверное сделай себе ветку от этой, модель создаем в competition_package/our_solution

могу предположить что начать стоит с трансформера, как раз множество полей DataPoint будет как вектор (токен) и должно работать.


